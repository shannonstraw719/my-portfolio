// Wait until the page has finished loading
document.addEventListener("DOMContentLoaded", () => {

    // Add a small message to the browser console
    console.log("Sam's portfolio is running!");

});